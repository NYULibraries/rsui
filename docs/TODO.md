## TODO

1) The eye toggle has no accessible name — an a11y gap

### Known gaps flagged by the suite

Browsing to an unknown collection renders a blank 200 page rather than a proper empty or
error state; `02` currently asserts only that it does not crash. See
`docs/production-readiness-plan.md`.
